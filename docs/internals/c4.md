# C4-style overview — ux-behavior

> **Diátaxis:** explanation · **Canonical:** `docs/internals/c4.md` · **Layer:** ux-behavior
> Map: [../INDEX.md](../INDEX.md).

Grounded in [../../DESIGN.md](../../DESIGN.md) and [ARCHITECTURE.md](ARCHITECTURE.md).
This layer owns product behavior → verified `list[Op]`.
It does **not** own raw HTML construction or wire codecs.

## Context

```mermaid
flowchart TB
  Author[Author]
  subgraph this["ux-behavior (this repo)"]
    Comp["Component + @action"]
    State["MorphState / RefState"]
    Disp["dispatch → list[Op]"]
  end
  Dom["ux-dom — HTML trees"]
  Channel["ux-channel — Cap crypto / wire"]
  Motion["ux-motion — presence plans"]
  Compose["ux-compose — product CLI"]
  Author --> Comp --> Disp
  Comp --> State
  Dom -.->|"optional look; not owned"| this
  Disp -->|"Ops; XOR with scene.enter"| Motion
  Channel -->|"Caps crypto only; isolation door"| this
  Compose -.->|"composition"| this
```

XOR (one Result): `morph(T)` XOR `scene.enter(T, html=…)`. Do not invent a sixth product.

# Documentation standard

Family contract: Diátaxis + Standard Readme + GitHub community health files.

Canonical pages: `docs/reference/`, `docs/guides/`, `docs/internals/`, `docs/tutorial/`, `docs/patterns/`.
Root `docs/API.md` and `docs/ARCHITECTURE.md` are **Moved stubs** — do not cite them.

Public names: `ux_behavior.__all__`. Out of scope this round: `ux-app`, `ux-motion-lib`.
